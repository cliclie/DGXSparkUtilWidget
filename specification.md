# 役割とゴール
あなたはC#およびWPF（.NET 9）のWindowsデスクトップ開発のエキスパートです。
以下の仕様書（specification.md）に基づき、コードの省略（「// ここに処理を書く」など）を一切行わず、そのままコピー＆ペーストで100%動作する完全なコードを生成してください。

# 出力言語の指定
- アプリのUIテキスト、ボタン、ラベル、設定ダイアログ、エラーメッセージはすべて「日本語」にしてください。
- コード内のコメントや解説もすべて「日本語」で記述してください。

# プロジェクト仕様書 (specification.md)

## 1. プロジェクト構成
- ターゲットフレームワーク: WPF (.NET 9)
- 出力形式: 自己完結型・単一ファイル実行形式（ランタイム内包の単一の .exe ファイル。外部依存なし）
- 必須パッケージ: `Microsoft.Web.WebView2` (NuGet)

## 2. メインウィンドウのスタイル（フレームレスと角丸）
- 枠なしの設定: `WindowStyle="None"`, `AllowsTransparency="True"`, `Background="Transparent"` を指定する。
- リサイズ不可: `ResizeMode="NoResize"` を指定する。
- 常に最前面: `Topmost="True"` を指定する。
- 角丸の実装: ウィンドウのルート要素に `Border` コントロールを配置し、`CornerRadius="10"`（または12）を設定して滑らかな角丸にする。内包する WebView2 が角丸からはみ出さないよう、適切にクリッピング（ClipToBounds）または背景の処理を施すこと。
- ウィンドウの移動: 背景の Border を左クリックしてドラッグした際、ウィンドウを移動できる処理（`this.DragMove()`）を実装する。
- プログラムアイコンはimages\DGXSpark.icoを使用する

## 3. メインコンテンツ
- 角丸 Border の内部に `Microsoft.Web.WebView2.Wpf.WebView2` コンポーネントを配置する。
- 初回起動時(設定値なし)のときは設定ダイアログを表示してurlを入力するよう促してください。

## 4. マウスオーバー時のフローティングUI（コントロールバー）
ユーザーがウィンドウ内にマウスを乗せた時（MouseEnter / MouseLeave、または XAMLのTriggerによるOpacity/Visibility制御）に、画面右上部に以下の操作コントロールがふわっと浮き上がる（フロート表示される）ようにする。
1. **ウィンドウ操作ボタン（左から順に右へ配置）**
   - 最小化ボタン: クリックでウィンドウを最小化する。
   - 最大化/元に戻すボタン: クリックで最大化と通常サイズをトグル（切り替え）する。
   - 閉じるボタン: クリックでアプリを完全に終了する。
2. **ハンバーガーメニューボタン**
   - コントロールバーの最も右側に配置する。
   - クリックすると、設定ダイアログ（またはウィンドウ内オーバーレイのモーダル）を表示する。

## 5. 設定ダイアログ（ポップアップ）
ハンバーガーメニューをクリックした際、別ウィンドウ（またはグリッド上に絶対配置された半透明のモーダルUI）として設定画面を表示する。
- **設定項目（日本語ラベル）**
  1. 接続先URL: 現在のWebView2のURLを変更・入力できる `TextBox`。
  2. 透過率（不透明度）: メインウィンドウ全体の `Opacity` を `0.2` から `1.0` の範囲で無段階にスライダー調整できる `Slider`。
- **アクションボタン**
  - 「保存」ボタン: 入力されたURLにWebView2をナビゲートし、透過率を反映させ、設定をローカル（または簡易的なjson/xmlファイル）に保存して閉じる。次回起動時にもこの設定を引き継ぐこと。
  - 「キャンセル」ボタン: 変更を破棄して閉じる。

# 出力要求ファイル
あなたは以下のファイルを省略せず、完全なコードとして順番に出力してください。
1. `DGXSparkUtilWidget.csproj` （WebView2パッケージおよび単一ファイルビルド用プロパティを含む）
2. `MainWindow.xaml` （カスタムスタイル、角丸、マウスオーバーで表示されるフローティングUIを含むUIレイアウト）
3. `MainWindow.xaml.cs` （ドラッグ移動、各種ボタンイベント、WebView2初期化、設定の読み込み/保存ロジック）
4. `SettingsDialog.xaml` および `SettingsDialog.xaml.cs` （またはMainWindow内に埋め込まれた設定モーダル用のXAMLとロジック）
5. システムの仕様や作業の記録はREADME.mdに都度記録してください。

# テスト
- URLを `http://192.168.0.110:8080` として接続確認してください。

