<Window x:Class="DGXSparkUtilWidget.ControlBarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="230" Height="32"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False">

    <Window.Resources>
        <!-- コントロールバーのボタン共通スタイル -->
        <Style x:Key="ControlButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#444444"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Width="46" Height="32">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#40000000"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#66000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- クローズボタンの専用スタイル（ホバーで赤くなる） -->
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource ControlButtonStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Width="46" Height="32">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#E81123"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#C50F1F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" Background="#99FFFFFF">
        <StackPanel Orientation="Horizontal">
            <!-- Web操作トグルボタン -->
            <Button x:Name="BtnWebToggle"
                    Style="{StaticResource ControlButtonStyle}"
                    ToolTip="Webページ操作を有効化">
                <Viewbox Width="18" Height="18">
                    <Path Data="M 6,3 L 6,19 L 11,14 L 15,21 L 18,19 L 14,12 L 20,10 Z"
                          Stroke="#444444" StrokeThickness="1.5"
                          Fill="Transparent"/>
                </Viewbox>
            </Button>

            <!-- ハンバーガーメニューボタン -->
            <Button x:Name="BtnMenu"
                    Style="{StaticResource ControlButtonStyle}"
                    ToolTip="設定">
                <Viewbox Width="18" Height="18">
                    <Path Data="M 0,4 L 18,4 M 0,9 L 18,9 M 0,16 L 18,16"
                          Stroke="#444444" StrokeThickness="2"/>
                </Viewbox>
            </Button>

            <!-- 最小化ボタン -->
            <Button x:Name="BtnMinimize"
                    Style="{StaticResource ControlButtonStyle}"
                    ToolTip="最小化">
                <Viewbox Width="18" Height="18">
                    <Path Data="M 4,16 L 24,16"
                          Stroke="#444444" StrokeThickness="2"/>
                </Viewbox>
            </Button>

            <!-- 最大化/元に戻すボタン -->
            <Button x:Name="BtnMaximizeRestore"
                    Style="{StaticResource ControlButtonStyle}"
                    ToolTip="最大化 / 元に戻す">
                <Viewbox Width="18" Height="18">
                    <Path x:Name="IconMaximize"
                          Data="M 5,5 L 23,5 L 23,23 L 5,23 Z"
                          Stroke="#444444" StrokeThickness="2"
                          Fill="Transparent"/>
                </Viewbox>
            </Button>

            <!-- 閉じるボタン -->
            <Button x:Name="BtnClose"
                    Style="{StaticResource CloseButtonStyle}"
                    ToolTip="閉じる">
                <Viewbox Width="18" Height="18">
                    <Path Data="M 8,8 L 20,20 M 20,8 L 8,20"
                          Stroke="#444444" StrokeThickness="2"/>
                </Viewbox>
            </Button>
        </StackPanel>
    </Border>
</Window>